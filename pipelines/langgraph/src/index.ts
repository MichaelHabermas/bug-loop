/**
 * LangGraph JS pipeline — Day 2.
 *
 * Will implement: ingest → detect → dedupe → reproduce → ticket → route
 * → {fix ⇄ verify | needs-human} → PR using @bug-loop/shared contracts.
 */
console.log(
  "[pipeline-langgraph] Day 2 stub — LangGraph JS implementation coming soon.",
);
