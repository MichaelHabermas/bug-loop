/**
 * Claude Agent SDK pipeline — Day 4.
 *
 * Will reimplement the same triage graph as Day 2 (LangGraph JS) using the
 * Claude Agent SDK, sharing @bug-loop/shared contracts.
 */
console.log(
  "[pipeline-agent-sdk] Day 4 stub — Claude Agent SDK implementation coming soon.",
);
